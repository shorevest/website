{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shorevest.com/assets/data/recruitment/roles.v1.schema.json",
  "title": "ShoreVest recruitment role manifest v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "generatedAtUtc", "roles"],
  "properties": {
    "schemaVersion": { "const": "1.0" },
    "generatedAtUtc": { "type": "string", "format": "date-time" },
    "roles": {
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/role" }
    }
  },
  "$defs": {
    "localizedRole": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "location", "team", "detailPath"],
      "properties": {
        "title": { "type": "string", "minLength": 1, "maxLength": 160 },
        "location": { "type": "string", "minLength": 1, "maxLength": 120 },
        "team": { "type": "string", "minLength": 1, "maxLength": 120 },
        "detailPath": { "type": "string", "minLength": 1, "maxLength": 240, "pattern": "^careers/[a-z0-9][a-z0-9-]*(_cn)?\\.html$" }
      }
    },
    "fileDefinition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["filePurpose", "required", "maxSizeBytes", "allowedExtensions", "allowedMimeTypes", "signatureValidation"],
      "properties": {
        "filePurpose": { "enum": ["cv", "cover_letter", "supporting_document"] },
        "required": { "type": "boolean" },
        "maxSizeBytes": { "type": "integer", "minimum": 1, "maximum": 52428800 },
        "allowedExtensions": {
          "type": "array",
          "minItems": 1,
          "items": { "enum": [".pdf", ".doc", ".docx"] },
          "uniqueItems": true
        },
        "allowedMimeTypes": {
          "type": "array",
          "minItems": 1,
          "items": {
            "enum": [
              "application/pdf",
              "application/msword",
              "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
            ]
          },
          "uniqueItems": true
        },
        "signatureValidation": {
          "type": "object",
          "additionalProperties": false,
          "required": ["required", "acceptedSignatures"],
          "properties": {
            "required": { "const": true },
            "acceptedSignatures": {
              "type": "array",
              "minItems": 1,
              "items": { "enum": ["pdf", "oleCompoundFile", "zipDocx"] },
              "uniqueItems": true
            }
          }
        }
      }
    },
    "workAuthorization": {
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled", "required", "jurisdictions"],
      "properties": {
        "enabled": { "type": "boolean" },
        "required": { "type": "boolean" },
        "jurisdictions": {
          "type": "array",
          "items": { "type": "string", "minLength": 2, "maxLength": 80 },
          "uniqueItems": true
        }
      }
    },
    "roleSpecificQuestion": {
      "type": "object",
      "additionalProperties": false,
      "required": ["questionId", "type", "required", "locales"],
      "properties": {
        "questionId": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{1,80}$" },
        "type": { "enum": ["short_text", "long_text", "single_select", "multi_select", "yes_no"] },
        "required": { "type": "boolean" },
        "locales": {
          "type": "object",
          "additionalProperties": false,
          "required": ["en", "zh-CN"],
          "properties": {
            "en": { "type": "string", "minLength": 1, "maxLength": 500 },
            "zh-CN": { "type": "string", "minLength": 1, "maxLength": 500 }
          }
        },
        "options": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["value", "locales"],
            "properties": {
              "value": { "type": "string", "minLength": 1, "maxLength": 80 },
              "locales": {
                "type": "object",
                "additionalProperties": false,
                "required": ["en", "zh-CN"],
                "properties": {
                  "en": { "type": "string", "minLength": 1, "maxLength": 160 },
                  "zh-CN": { "type": "string", "minLength": 1, "maxLength": 160 }
                }
              }
            }
          }
        }
      }
    },
    "role": {
      "type": "object",
      "additionalProperties": false,
      "required": ["roleId", "status", "locales", "employmentType", "applicationEnabled", "files", "screening"],
      "properties": {
        "roleId": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]{2,80}$" },
        "status": { "enum": ["draft", "active", "closed", "archived"] },
        "locales": {
          "type": "object",
          "additionalProperties": false,
          "required": ["en", "zh-CN"],
          "properties": {
            "en": { "$ref": "#/$defs/localizedRole" },
            "zh-CN": { "$ref": "#/$defs/localizedRole" }
          }
        },
        "employmentType": { "enum": ["Full-time", "Part-time", "Internship", "Contract"] },
        "applicationEnabled": { "type": "boolean" },
        "files": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/fileDefinition" }
        },
        "screening": {
          "type": "object",
          "additionalProperties": false,
          "required": ["workAuthorization", "roleSpecificQuestions"],
          "properties": {
            "workAuthorization": { "$ref": "#/$defs/workAuthorization" },
            "roleSpecificQuestions": {
              "type": "array",
              "items": { "$ref": "#/$defs/roleSpecificQuestion" }
            }
          }
        }
      }
    }
  }
}
