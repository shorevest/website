'use strict';const Ajv=require('ajv/dist/2020');const addFormats=require('ajv-formats');const manifest=require('../src/roles.v1.json');const schema=require('../src/roles.v1.schema.json');const ajv=new Ajv({allErrors:true});addFormats(ajv);const validate=ajv.compile(schema);if(!validate(manifest)){console.error(validate.errors);process.exit(1);}console.log('recruitment functions bundled manifest valid');
